# run dev command

set PORT=5173 && set BASE_PATH=/ && pnpm --filter @workspace/newtab-extension run dev

# run build command

set PORT=5173 && set BASE_PATH=/ && pnpm --filter @workspace/newtab-extension run build